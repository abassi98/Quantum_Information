      PROGRAM add_numbers
      !This program accepts two numbers as the input and compute their sum
      IMPLICIT NONE
      INTEGER*2 a,b,sum  !we have computed first with the type INTEGER*2
      a=2000000
      b=1
      sum=a+b
      sum=a+b
      PRINT *, "The sum of the two number is ", sum
      STOP
      END 
